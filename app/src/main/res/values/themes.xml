<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PaymaartMerchant" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.PaymaartMerchant" parent="Base.Theme.PaymaartMerchant" />

    <style name="Guide.Bottom.Sheet.Style" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/primaryColor</item>
    </style>

</resources>